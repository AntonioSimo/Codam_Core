docker exec -it mariadb mysql -u root -p //accesso al Mariadb container e inserire password root

MARIADB COMMANDS:
SHOW DATABASES; //mostra il databese che si utilizza
USE asimone_inception; //se il databese esiste, si puo' fare accesso e si lavora su di esso
SHOW TABLES; // mostra le tabelle (se esistono tramite script SQL)